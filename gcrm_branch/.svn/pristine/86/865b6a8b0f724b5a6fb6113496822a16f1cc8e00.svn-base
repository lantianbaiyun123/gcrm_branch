define(['app'], function (app) {
    app.registerFilter('BoolValueFilter', function () {
        return function ( input ) {
            var ret = input ? 'YES' : 'NO';
            return ret;
        };
    });
});